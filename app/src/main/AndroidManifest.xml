<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tanvi.moviemania">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activites.DisplayActivity" />
        <activity
            android:name=".Activites.MainActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme">

        </activity>
        <activity
            android:name=".Activites.AboutAMovieActivity"
            android:label="@string/title_activity_about_amovie"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".Activites.MoreAboutActivity"
            android:theme="@style/AppTheme"></activity>
        <activity android:name=".Activites.TempActivity"
            android:theme="@style/AppTheme"
            android:configChanges="orientation|screenSize"

            >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>